# Seed Data

Place JSON seed files here for development/testing.

Usage: `cd apps/api && uv run python -m seeds.load`

## Files
- `capabilities.json` — Base capability taxonomy
- `opportunities.json` — Sample opportunities (development only)
